# Alpha.5 release verification trigger

Final observable verifier run for immutable `v0.1.0-alpha.5`: verify release hashes, regenerate repository screenshots, and replace the README preview links with the version-controlled WPF snapshots.
