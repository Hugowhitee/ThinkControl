# Alpha.5 release verification trigger

Run the observable verifier against the immutable `v0.1.0-alpha.5` release. The verifier also generates the three version-controlled README previews from the same WPF snapshot renderer.
