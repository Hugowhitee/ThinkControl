<Window x:Class="ThinkControl.UI.NotificationCenterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:ThinkControl.UI.Controls"
        Title="ThinkControl notifications"
        Width="560" Height="560"
        MinWidth="500" MinHeight="430"
        MaxWidth="720" MaxHeight="760"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="False"
        Background="{DynamicResource Tc.Window}"
        Foreground="{DynamicResource Tc.Text}"
        FontFamily="Segoe UI"
        Icon="Assets/ThinkControl.ico">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Notifications" FontSize="22" FontWeight="SemiBold" />
                <TextBlock Text="Hardware status, setup steps and compatibility discoveries in one place."
                           Foreground="{DynamicResource Tc.TextMuted}" FontSize="10.5" Margin="0,5,0,0" />
            </StackPanel>
            <Button Grid.Column="1" Content="Refresh" Style="{StaticResource TcButton}" Padding="10,5" VerticalAlignment="Center" Click="Refresh_Click" />
        </Grid>

        <Border Grid.Row="1" Background="{DynamicResource Tc.SurfaceAlt}" BorderBrush="{DynamicResource Tc.Border}" BorderThickness="1"
                CornerRadius="4" Padding="10,8" Margin="0,16,0,12">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                <Ellipse x:Name="SummaryDot" Width="7" Height="7" Fill="{DynamicResource Tc.Warning}" VerticalAlignment="Center" Margin="0,0,9,0" />
                <TextBlock x:Name="SummaryText" Grid.Column="1" Text="Checking hardware providers…" Foreground="{DynamicResource Tc.TextMuted}" FontSize="10.5" TextWrapping="Wrap" />
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel x:Name="MessagesPanel" Margin="0,0,3,0" />
        </ScrollViewer>

        <Grid Grid.Row="3" Margin="0,14,0,0">
            <TextBlock Text="Nothing is installed, changed or shared until you choose an action."
                       Foreground="{DynamicResource Tc.TextFaint}" FontSize="9.5" VerticalAlignment="Center" />
            <Button Content="Done" Style="{StaticResource TcButton}" MinWidth="84" HorizontalAlignment="Right" Click="Done_Click" />
        </Grid>
    </Grid>
</Window>