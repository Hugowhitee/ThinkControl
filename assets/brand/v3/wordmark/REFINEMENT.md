# Wordmark refinement

The approved v3 custom C and red dot remain unchanged. The final production wordmark keeps the original outlined `Think` geometry and renders the outlined trailing `ontrol` at 87.5% scale with a baseline-preserving offset, matching the approved visual hierarchy used by the app. Production assets are path-based and do not depend on installed fonts.
