Canonical Windows icon assets from the approved ThinkControl v3 pack live in this folder.

`ThinkControl_setup.ico` is an Inno Setup compatibility container built from the exact 256 px image frame in the approved `ThinkControl.ico`. Its artwork is pixel-identical to the v3 source; only the ICO directory metadata is normalized so Inno can embed it in the setup executable.
