<div align="center">
  <picture>
    <source media="(prefers-color-scheme: dark)" srcset="assets/brand/v3/wordmark/ThinkControl_wordmark_dark.svg">
    <source media="(prefers-color-scheme: light)" srcset="assets/brand/v3/wordmark/ThinkControl_wordmark_light.svg">
    <img alt="ThinkControl" src="assets/brand/v3/wordmark/ThinkControl_wordmark_light.svg" width="430">
  </picture>

  <p><strong>A compact ThinkPad-first Windows control app for power, cooling, sensors, display, audio, keyboard, touchpad and battery telemetry.</strong></p>

  [![Windows CI](https://github.com/Hugowhitee/ThinkControl/actions/workflows/ci.yml/badge.svg?branch=main)](https://github.com/Hugowhitee/ThinkControl/actions/workflows/ci.yml)
  [![Release](https://img.shields.io/github/v/release/Hugowhitee/ThinkControl?include_prereleases&label=release)](https://github.com/Hugowhitee/ThinkControl/releases)
  [![Downloads](https://img.shields.io/github/downloads/Hugowhitee/ThinkControl/total?label=downloads)](https://github.com/Hugowhitee/ThinkControl/releases)
  [![License](https://img.shields.io/badge/license-source--available-555)](LICENSE)

  **[Download](https://github.com/Hugowhitee/ThinkControl/releases)** ·
  **[Device support](docs/DEVICE-SUPPORT.md)** ·
  **[Documentation](docs/README.md)** ·
  **[Report a bug](https://github.com/Hugowhitee/ThinkControl/issues/new?template=bug-report.yml)**

  <br><br>
  <a href="https://buymeacoffee.com/hugowhite">
    <img src="https://cdn.buymeacoffee.com/buttons/v2/default-yellow.png" alt="Buy Me a Coffee" height="44">
  </a>
</div>

## ThinkControl alpha.28

ThinkControl is a lightweight Windows 10/11 companion focused first on Lenovo and ThinkPad laptops. It combines controls normally spread across Windows Settings, Lenovo utilities and monitoring tools into a fast Compact view and a resizable Full view.

**Current prerelease:** `v0.1.0-alpha.28`

**Verified low-level reference:** ThinkPad X9-15 Gen 1 (`21Q6` / `21Q7`)  
**Platform:** Windows 10 version 2004 (build 19041) or newer, x64 · .NET 10

Windows-safe controls can work on more systems, while direct EC/fan and OEM controls stay capability-gated. ThinkControl does not invent RPM values, PWM percentages, sensor readings or guessed hardware registers.

## Interface

<p align="center">
  <a href="https://github.com/Hugowhitee/ThinkControl/releases/download/v0.1.0-alpha.28/ui-overview.png">
    <img src="https://github.com/Hugowhitee/ThinkControl/releases/download/v0.1.0-alpha.28/ui-overview.png" alt="ThinkControl alpha.28 interface overview" width="920">
  </a>
</p>
<p align="center"><sub>Compact and Full views plus the main control, telemetry and setup surfaces. The complete dark/light, minimum/normal/wide matrix is generated by CI rather than duplicated here as a long screenshot list.</sub></p>

ThinkControl has two primary surfaces:

- **Compact view** — quick telemetry and the controls you change most often.
- **Full view** — Home, Performance, Fans, Display, Audio, Keyboard, Battery, Touchpad, System, Updates and Settings.

The release pipeline renders the real WPF interface across dark/light themes and multiple viewport sizes. It also runs real Compact ↔ Full lifecycle smoke tests so shell regressions are not hidden by static screenshots.

## Install

Download the newest setup from **[GitHub Releases](https://github.com/Hugowhitee/ThinkControl/releases)**:

```text
ThinkControl-Setup-0.1.0-alpha.28.exe
```

For a normal install, Setup is the only file you need. Each public prerelease also includes the updater payload, `SHA256SUMS.txt` and `ui-overview.png`.

Updates are explicit: ThinkControl downloads Setup + Payload + checksums, verifies SHA-256, then asks Windows for elevation. Background update checks never install or open UAC by themselves.

## What alpha.28 changes

Alpha.28 is a reliability and consistency pass built from real alpha.27 crash and visual-QA findings.

- **The alpha.27 tooltip crash is fixed at its source.** ThinkControl no longer creates the unsupported WPF `ToolTip` / `StaysOpen=false` combination reported in issue #55.
- **Crash reports survive repeated crashes.** Fatal reports are stored as durable per-crash records with stable IDs, signatures, occurrence counts and explicit Pending / Opened / Reported / Dismissed states. New crashes do not overwrite unresolved ones, and opening a GitHub draft no longer deletes the local record.
- **Typography is deterministic instead of runtime-normalized.** Shared semantic roles now own page titles, sections, body copy, labels, controls and values; release QA validates the rendered hierarchy without mutating it.
- **Compact and Full share one utility-order contract.** Notifications and the deliberate view-mode control use the same ordering and hit-target logic in both shells.
- **Compact selectors use one sizing/template contract.** Font size, padding, presenter height and minimum control height now move together so larger readable typography does not clip the dropdown surface.
- **Home is more deliberate.** The battery metric uses a better text/gauge balance and internal destination cards share one restrained navigation affordance while Windows Settings links retain their separate external-link treatment.
- **Touchpad gestures share semantic visuals and icons.** Volume, Brightness, Media scrub, Track control and Open Compact resolve through one action catalog; the visualizer owns spacing, inversion, motion type and active feedback.
- **Inward gestures are physically distinct.** Open Compact is recognized and rendered as an edge-to-interior gesture rather than pretending to be another along-edge slider.
- **Track control is guarded against accidental Play/Pause.** Previous / optional center Play-Pause / Next uses the shared renderer, while center activation requires a deliberate low-movement hold and release.
- **Audio navigation remains off the dispatcher.** Repeated page opens reuse cached state immediately and background CoreAudio/Dolby probing is covered by real shell smoke timing.
- **Visual QA is stricter.** The release renderer includes minimum/normal/wide views plus verified/new-device diagnostics, preserved crash queues and active Media scrub / inward / Track-control states, with read-only typography and layout assertions.

Hardware-specific fan, haptic and OEM control behavior remains capability-gated; this release does not claim physical hardware fixes solely from CI.

## Main capabilities

| Area | What ThinkControl provides |
| --- | --- |
| **Home** | Live Battery, CPU, fan/RPM, power and sensor overview plus quick controls |
| **Performance** | Separate battery and plugged-in preferences using Windows power integration |
| **Fans** | Lenovo/OEM Auto, verified manual levels and supervised custom curves where a writable provider passes validation |
| **Battery** | Watts, Wh, health, ETA, temperature when genuinely exposed, and compact local charge/discharge history |
| **Display** | Brightness, adaptive brightness, refresh rate, automatic refresh switching and Windows display shortcuts |
| **Audio** | Windows output/microphone control plus semantic Dolby controls where the installed DAX provider safely exposes them |
| **Keyboard** | Hardware brightness/effects only where a verified Lenovo provider is available |
| **Touchpad** | Live contact visualization, configurable edge gestures, haptics and bounded gesture feedback |
| **System** | Device/provider state, repair flow and detailed sensor telemetry |
| **Updates** | Shared update state, SHA-256 verification and explicit installer handoff |
| **Settings** | App behavior, appearance, diagnostics/privacy, battery-history retention and reset controls |

## Hardware safety and device support

ThinkControl grows support from broad to specific:

`Windows generic → OEM generic → product family → exact model`

Profiles decide which providers are reasonable to probe. Providers own implementation, readback, lifecycle and write safety. Low-level controls remain visible but unavailable until the exact capability is detected and validated.

On the verified X9-15 profile:

- direct fan writes stay restricted to reviewed machine types;
- writes are read back and supervised;
- failed/disposed manual ownership returns to Lenovo/OEM Auto where supported;
- real provider telemetry is preferred over fallback probes;
- sensor/provider failure is reported explicitly rather than replaced by synthetic values.

On an unverified laptop, ThinkControl can quietly learn provider/control compatibility while the user naturally exercises the app. Known devices skip that compatibility-learning workflow and load normally.

See **[Device support](docs/DEVICE-SUPPORT.md)** and **[Hardware safety](docs/HARDWARE-SAFETY.md)**.

## Diagnostics and privacy

Diagnostics are separated into three concerns:

- **Compatibility learning** for an unknown device, until the useful evidence is stable.
- **Crash recovery**, stored locally so a fatal crash can be reviewed on the next healthy launch.
- **Troubleshooting data**, kept local and bounded for support/debugging.

Compatibility reports exclude serial numbers, Windows usernames, hostnames, personal paths and raw personal logs. Nothing is uploaded automatically. Opening a pre-filled GitHub report is an explicit action, and the same semantic report is remembered as handled until new evidence appears.

See **[Diagnostics & privacy](docs/DIAGNOSTICS.md)**.

## Touchpad behavior

Continuous actions are bounded and coalesced rather than issuing an OS write for every raw touch frame. Media seeking uses an accumulated target and controlled write cadence so slow movement can stay precise without flooding media sessions.

The visual trail represents actual continuous contact. A new contact starts a new segment; an implausibly large physical jump also breaks the segment instead of drawing a fake connecting line.

Previous/Next can optionally expose Play/Pause in the center. A skip gesture wins first; center Play/Pause only commits after a near-neutral hold and release, so normal sliding does not accidentally resume media.

## Audio and Dolby

ThinkControl keeps the normal Windows audio path usable even if Dolby integration is unavailable. Output/input discovery is performed away from the UI thread and coalesced so revisiting Audio does not repeatedly block the page on CoreAudio COM activation.

Direct Dolby controls are enabled only when the installed DAX provider exposes a semantic interface ThinkControl can verify. Unsupported DAX builds remain explicit; ThinkControl does not guess private Dolby profile IDs.

## Development and validation

Windows CI restores and builds the solution, runs core tests, executes the real shell-transition smoke and renders the WPF visual-QA matrix before packaging.

```powershell
dotnet restore ThinkControl.slnx
dotnet build ThinkControl.slnx -c Release
.\tools\visual-qa.ps1
```

Packaging and installer workflows additionally validate payload construction, clean install, service start/IPC, in-place updater compatibility and uninstall cleanup before a prerelease is promoted.

Automated CI cannot prove physical-device behavior. Fan ownership/RPM, OEM keyboard readback, touchpad HID behavior, Dolby DAX behavior and haptic response still require validation on the actual target hardware before that exact driver/firmware combination is treated as physically confirmed.

See **[Documentation](docs/README.md)** · **[Device support](docs/DEVICE-SUPPORT.md)** · **[X9-15 research](docs/research/x9-15-gen1.md)**.
